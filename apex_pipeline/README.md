# APEX holography pipeline

The standard physical optics based optimization for the APEX telescope is composed by two separate steps. In the first step we run an optimization to obtain the geometrical parameters that best fit the measured beam pattern (defocus, subreflector rotation, receiver horn offsets and rotations, gaussian beam parameters). In the second step we keep the geometrical parameter fixed and we deform the main dish to obtain the panel adjustments.


